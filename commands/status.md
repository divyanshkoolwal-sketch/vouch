---
description: Show Vouch status for this repo — config, active intent, and the most recent verification findings.
---

Call the `get_status` tool from the `vouch` MCP server and present the result clearly: whether Vouch is configured, the active intent (if any), which tiers run and which can block, and the most recent verification findings (with their vouch ids so the user can dismiss any false positives).
